document.addEventListener("DOMContentLoaded",()=>{const grid=document.getElementById("productGrid");grid.innerHTML="";(window.products||[]).forEach((product,idx)=>{const card=document.createElement("div");card.className="product-card";card.style.animationDelay=`${0.11*idx}s`;card.innerHTML=`<img src="${product.image}" alt="${product.name}"><div class="product-info"><h3 class="product-title">${product.name}</h3><p class="product-desc">${product.description}</p><p class="product-price">${product.price}</p></div>`;grid.appendChild(card)})});
